@inject NavigationManager NavigationManager

<MudButton Href="/"
           Target="_self" Variant="Variant.Text"
           Color="@(NavigationManager.Uri.EndsWith("/") ? Color.Primary : Color.Inherit)"
           StartIcon="@Icons.Material.Filled.Home"
           IconSize="Size.Medium">
    Home
</MudButton>